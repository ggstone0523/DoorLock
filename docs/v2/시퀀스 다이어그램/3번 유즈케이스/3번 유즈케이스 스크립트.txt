title 3번 유즈케이스

actor "User" as user
participant "TouchGFX[Screen1View]" as touchgfx
participant "numberkey" as numberkey
participant "sound" as sound
participant "motor" as motor

loop 시용자가 누른 숫자키 != * 키
user->touchgfx:숫자키를 누른다
touchgfx->numberkey:NumberKeyMessageBuffer[MQ](char)
numberkey->sound:SoundEventGroup[Event](SOUND_CLICK_NUMBER_KEY)
end

user->touchgfx:* 키를 누른다
touchgfx->numberkey:NumberKeyMessageBuffer[MQ](char)
alt 시나리오(기본)-1 / 시나리오(대안)-1
numberkey->motor:MotorEventGroup[Event](MOTOR_CONTROL)
numberkey->sound:SoundEventGroup[Event](SOUND_SUCCESS)
else 시나리오(예외상황)-1
numberkey->sound:SoundEventGroup[Event](SOUND_FAIL)
end